import {profileInitialState} from "./profileInitialState";

export const profileReducer = (state = profileInitialState, action: any) => {
    switch (action.type) {


        default: {
            return state;
        }
    }
};
