import {recoverPasswordInitialState} from "./recoverPasswordInitialState";

export const recoverPasswordReducer = (state = recoverPasswordInitialState, action: any) => {
    switch (action.type) {


        default: {
            return state;
        }
    }
};
