import {newPasswordInitialState} from "./newPasswordInitialState";

export const newPasswordReducer = (state = newPasswordInitialState, action: any) => {
    switch (action.type) {


        default: {
            return state;
        }
    }
};
